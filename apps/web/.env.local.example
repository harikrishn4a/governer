# All environment variables are centralised in governer/.env (root).
# next.config.ts loads governer/.env automatically — no .env.local needed.
# If you need to override a specific var for local web dev only, add it here.
