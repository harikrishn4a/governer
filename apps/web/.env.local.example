# All environment variables are centralised in procure/.env (root).
# next.config.ts loads procure/.env automatically — no .env.local needed.
# If you need to override a specific var for local web dev only, add it here.
