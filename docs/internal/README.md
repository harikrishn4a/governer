# Internal / archived notes

These files are the build history and agent-harness scaffolding from the original
hackathon sprint — kept for provenance, not needed to run or contribute to AgentBid.

- `AGENTS.md`, `CLAUDE.md` — agent-harness operating instructions
- `PROGRESS.md`, `SESSION-HANDOFF.md`, `TASK.md`, `feature_list.json`, `features.md` — sprint tracking
- `DECISIONS.md`, `CONSTRAINTS.md`, `GAP-ANALYSIS.md`, `VALIDATION-REPORT.md` — design/decision logs
- `B1/B2/B3-COMPLETE.md`, `F1-COMPLETE.md` — per-task completion reports
- `ARCHITECTURE.md` — early renderer-layer boundary note (see the README for the current overview)
- `init.sh`, `verify.sh`, `deploy-ec2.sh`, `Makefile` — sprint dev/deploy scaffolding (the EC2 script targets a since-disabled sandbox)
- everything else — misc process docs

To run or develop AgentBid, see the top-level [`README.md`](../../README.md).
